# 量化项目工作流汇总 (2026-08-27)

## 一、每天要做的事 (日常三步)

```bash
# ① 更新行情数据 (增量, 秒级; 已存在的自动跳过)
.venv/bin/python scripts/save_watchlist.py --symbols 00700.SEHK,AAPL.NASDAQ,000001.SZSE

# ② 写/改策略 (脚手架生成, 去 strategies/ 目录改逻辑)
.venv/bin/python scripts/new_strategy.py --name 你的策略名 --kind ma

# ③ 回测
.venv/bin/python run_backtest.py --strategy 你的策略名 --symbol 00700.SEHK --start 2022-01-01 --csv
```

可选工具:
```bash
.venv/bin/python batch_backtest.py --symbols ... --strategies ...   # 批量对比
.venv/bin/python scripts/show_data.py --symbol 00700.SEHK --days 20  # 看数据
.venv/bin/python scripts/list_symbols.py --market US                 # 查股票代码
```

所有命令都在项目目录下跑: `cd /Users/xucongyong/mathematics/120code/quant`

## 二、这一天的整体流程 (怎么搭起来的)

```
akshare 抓行情 → 存进远程 PostgreSQL quant 库 → 回测从同一库读数据
    save_watchlist.py       quant_data 表(697万行)       run_backtest.py
```

## 三、已解决的问题清单

| # | 问题 | 解决办法 |
|---|---|---|
| 1 | 每天怎么快速写策略 | `scripts/new_strategy.py` 脚手架, 5种模板(ma/boll/rsi/momentum/template) |
| 2 | 怎么一键回测 | `run_backtest.py` CLI, 自动识别市场默认费率(A股万3/港股千1.5/美股万5) |
| 3 | akshare 数据保存到哪 | 不存文件, 转成 vnpy BarData 用 `db.save_bar_data()` 直接进 PG 库 |
| 4 | 已存在的价格要不要跳过 | 不用! upsert 自动去重; 增量只拉"最新日期-5天"到现在 |
| 5 | 港股/美股东财接口被拒 | `save_watchlist.py` 自动降级到新浪接口 |
| 6 | 数据散在业务库太乱 | 迁移到专用 `quant` 库 (697万行+2872行), vt_setting 已切换 |
| 7 | 表名 dbbardata 太丑 | 物理表改名为 `quant_data`/`quant_data_sync`, 驱动同步打补丁 |
| 8 | 怎么保证数据不重复 | 表上唯一索引 (symbol+exchange+interval+datetime), 实测0重复 |
| 9 | 股票代码去哪查 | `scripts/list_symbols.py` + psql (全库2873个代码) |
| 10 | 迁移太慢 | 删除重复的唯一索引, 提速10倍, 14分钟跑完 |
| 11 | 迁移中断丢数据 | 修复 autocommit + ON CONFLICT DO NOTHING 幂等重试 |

## 四、查数据库 (psql)

```bash
export PGPASSWORD=1121hotsren
# 专用库: quant | 表: quant_data(行情) / quant_data_sync(同步摘要)
/opt/homebrew/opt/libpq/bin/psql -h v.xucongyong.com -U postgres -d quant \
  -c "SELECT symbol,exchange,count(*) FROM quant_data.quant_data GROUP BY 1,2"
```

## 五、必须记住的注意点

1. **vnpy 驱动已打补丁**: 表名改为 quant_data/quant_data_sync, 不再建tick空表。
   若重装/升级 `vnpy_postgresql`, 需在 `.venv/lib/python3.12/site-packages/vnpy_postgresql/postgresql_database.py` 重新打补丁(见 AGENTS.md)
2. 数据最新同步到 2026-08-27
3. 股票清单用 `watchlist.txt` 文件 (每行一个代码), watchlist 表已删

## 六、还没做的(可选)

- `daily.py` 一条龙: 同步+批量回测+报告 一条命令 (想省事再做)
- BTC AI 策略: `run_btc_ai.py` (yfinance + LightGBM, 独立流程)
